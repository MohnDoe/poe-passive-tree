name: CI

on:
  pull_request:
    branches: [main]

jobs:
  build:
    name: Type check & unit tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2 

      - name: Install dependencies
        run: bun install

      - name: Type check
        run: bun run type-check

      - name: Run unit tests
        run: bun test:unit -- --run --coverage
